import { IfConditionDirective } from './if-condition.directive';

describe('IfConditionDirective', () => {
  it('should create an instance', () => {
    const directive = new IfConditionDirective();
    expect(directive).toBeTruthy();
  });
});
